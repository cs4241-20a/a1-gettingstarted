var circle = document.getElementById('circle');
    
  function animate(){
    circle.style.transform = 'translate(50px, 20px)';
  }

      if(document.getElementById('click').clicked){
        animate();
      }